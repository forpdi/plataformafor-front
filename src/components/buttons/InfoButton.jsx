import React from 'react';

import TransparentIconButton from 'forpdi/src/components/buttons/TransparentIconButton';

const InfoButton = props => (
  <TransparentIconButton {...props} icon="info-circle" />
);

export default InfoButton;
